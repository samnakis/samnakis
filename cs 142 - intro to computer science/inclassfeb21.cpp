#include <iostream>

int main()
{
	for(int count = 10; count > 0; count--) 
	//for(dataType variableName = 0; variableName > 0; variableName--)
	{
		//statement(s) go here
		//loop body
		std::cout << count << "\n";
	}
	
	std::cout << "blast off!!" << "\n";

	return 0;
}